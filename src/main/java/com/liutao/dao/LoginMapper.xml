<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liutao.dao.LoginMapper">

	<resultMap type="com.liutao.bean.UserBean" id="userMap">
		<result property="userId" column="user_id" />
		<result property="userName" column="user_name" />
		<result property="userPwd" column="user_pwd" />
		<result property="userMail" column="user_mail" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
	</resultMap>


	<select id="findUserByUserMail" parameterType="String"
		resultMap="userMap">
		SELECT
		USER_ID
		,USER_NAME
		,USER_PWD
		,USER_MAIL
		,CREATE_TIME
		,UPDATE_TIME
		FROM
		USER
		WHERE
		USER_MAIL = #{userMail}
		AND
		USER_PWD =
		#{userPwd}
	</select>

	<!-- <insert id="addUserCar" parameterType="com.openlab.bean.Car"> insert 
		into car (cmoney,cname,cimage,gid,uid) values (#{cmoney},#{cname},#{cimage},#{gid},#{uid}) 
		</insert> <select id="findByIdUserCar" parameterType="com.openlab.bean.Car" 
		resultType="com.openlab.bean.Car"> select * from car where gid=#{gid} and 
		uid=#{uid} </select> <update id="updateUserCar" parameterType="com.openlab.bean.Car"> 
		update car set cnum=#{cnum} where cid = #{cid} </update> <select id="findAllUserCar" 
		resultType="com.openlab.bean.Car" parameterType="com.openlab.bean.Car"> select 
		* from car where uid=#{uid} </select> 批量删除 <delete id="delAllUserCar" parameterType="string"> 
		delete from car where cid in <foreach item="cid" index="index" collection="arr" 
		open="(" separator="," close=")"> #{cid} </foreach> </delete> 删除单个购物车中的商品 
		<delete id="delUserCar" parameterType="int"> delete from car where cid =#{cid} 
		</delete> 删除用户购物车中所有的商品 <delete id="delUserCars" parameterType="string"> 
		delete from car where uid in <foreach item="uid" index="index" collection="arr" 
		open="(" separator="," close=")"> #{uid} </foreach> </delete> <select id="findReadyOrders" 
		resultType="com.openlab.bean.Car" parameterType="string"> select * from car 
		where cid in <foreach item="cid" index="index" collection="arr" open="(" 
		separator="," close=")"> #{cid} </foreach> </select> <update id="updateCar" 
		parameterType="com.openlab.bean.Car"> update car set cmoney=#{cmoney},cstatus=#{cstatus} 
		where gid=#{gid} </update> -->
</mapper>